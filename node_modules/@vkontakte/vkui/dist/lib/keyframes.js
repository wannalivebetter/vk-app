"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = keyframes;

function keyframes(name, obj) {
  if (!name || !obj) {
    return '';
  }

  var selectors = Object.keys(obj).map(function (key) {
    return key + ' {' + Object.keys(obj[key]).map(function (k) {
      return k + ':' + obj[key][k];
    }).join(';') + '}';
  }).join('');
  return "@-webkit-keyframes ".concat(name, " {").concat(selectors, "} @keyframes ").concat(name, " {").concat(selectors, "}");
}
//# sourceMappingURL=keyframes.js.map